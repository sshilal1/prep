https://reactjs.org/docs/installation.html#creating-a-new-application

draw up index.hmtl and app.js
npm i react, react-dom, webpack
npm i --save-dev babel-loader babel-core babel-preset-env babel-preset-es2015 babel-preset-react babel-preset-stage-0
add this to webpack config
module: {
  loaders: [
    { test: /\.js$/, exclude: /node_modules/, loader: "babel-loader", options: {} }
  ]
}
	module: {
	  rules: [
	    { 
				test: /\.js$/, 
				exclude: /node_modules/,
				loader: "babel-loader",
				options: {
					presets: ['env',"es2015", "stage-0", "react"]
				}
			}
	  ]
	}
	
module: {
  loaders: [
    { test: /\.js$/, exclude: /node_modules/, loader: "babel-loader" }
  ]
}